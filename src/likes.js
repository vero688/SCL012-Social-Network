//  // INTENTO DE LIKES -- DIANA //

//  const postLike = () => {
//    let post = firebase.firestore().collection('post')
//     get().where(doc.id ===id)

//     if([].contains (user.displayName)) {
//       [].filter(name => name) == user.displayName;
//       post.update(
//       {likes:[]});

//     } else{
//       [].push(user.displayName);
//       post.update(
//         {likes:[]});
//       }
//         let contadorDeLikes = post.likes.length;

//     }